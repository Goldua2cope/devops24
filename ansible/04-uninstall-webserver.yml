---
- name: Install a webserver
  hosts: web
  become: true
  tasks:
    - name: Stop and Disable nginx at startup
      ansible.builtin.service:
        name: nginx
        enabled: false
        state: stopped

    - name: Remove nginx
      ansible.builtin.package:
        name: nginx
        state: absent
